# Undo Reset Revert

| Goal | Command |
|------|---------|
| Unstage | `git restore --staged <file>` |
| Discard working tree | `git restore <file>` |
| Undo commit (soft) | `git reset HEAD~1` |
| Revert public commit | `git revert <sha>` |

